[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

# Avalanche Fuji (C-Chain testnet) — chain id 43113
[rpc_endpoints]
fuji = "${FUJI_RPC_URL}"
